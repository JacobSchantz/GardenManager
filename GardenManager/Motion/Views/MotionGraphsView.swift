import SwiftUI
import Charts

struct MotionGraphsView: View {
    @EnvironmentObject var motionTracker: MotionTracker
    
    var body: some View {
        ScrollView {
            VStack(spacing: 20) {
            }
            .padding(.vertical)
        }
    }
}
